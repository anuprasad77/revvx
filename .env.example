# Replace the placeholder WhatsApp number in app/page.tsx before deployment.
# Format: country code + number, without a plus sign (e.g. 919876543210)
NEXT_PUBLIC_WHATSAPP_NUMBER=916362777677

# Supabase connection values for moving catalogue and inquiries from sample data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
